---
title: "Advent9"
author: "Mandy Grawl"
date: "2025-12-09"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```


```{r}
df <- read.csv("Advent9.txt", header = FALSE)
colnames(df) <- c("x", "y")

n <- nrow(df)

max_area <- -1
best_i <- NA
best_j <- NA

for (i in 1:(n-1)) {
  for (j in (i+1):n) {
    dx <- abs(df$x[i] - df$x[j]) + 1
    dy <- abs(df$y[i] - df$y[j]) + 1
    area <- dx * dy

    if (area > max_area) {
      max_area <- area
      best_i <- i
      best_j <- j
    }
  }
}

max_area

```
could not for the life of me figure out how to do part two. I apologize


